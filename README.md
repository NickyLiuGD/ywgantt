# 在线云端协作甘特图 (Cloud Gantt Chart)

访问 https://{your-username}.github.io/ywgantt/   
或部署至 Cloudflare Pages

**更新**: 2025-11  
**版本**: Epsilon 18 (Alpha) - Cloudflare KV 集成版

---

<details>
<summary>📁 <strong>项目文件结构（模块化架构）</strong></summary>

```text
ywgantt/
├── index.html                    # 主HTML文件（模块化加载器 + 依赖验证）
├── css/
│   ├── base.css                  # 基础重置与变量
│   ├── utilities.css             # 动画、渐变、辅助类
│   ├── components.css            # UI组件（圆形工具栏、设置面板、文件管理弹窗）
│   ├── popup-menu-base.css       # 统一弹窗基础样式
│   ├── gantt-layout.css          # 甘特图网格、侧边栏布局
│   ├── gantt-interactive.css     # 交互元素（任务条、拖拽手柄、SVG箭头）
│   └── pert-styles.css           # PERT视图专用样式（节点、连线手柄）
├── js/
│   ├── utils/                    # 🔧 工具函数模块
│   │   ├── date-utils.js         #   - 日期计算（支持工作日/自然日双模式）
│   │   ├── time-scale-utils.js   #   - 时间刻度计算（日/周/月/全貌视图）
│   │   ├── log-utils.js          #   - 日志系统
│   │   ├── data-utils.js         #   - ID生成、SVG路径、深拷贝
│   │   ├── kv-storage.js         #   - Cloudflare KV API 封装
│   │   └── data-loader.js        #   - JSON数据解析与格式化
│   │
│   ├── gantt/                    # 📊 甘特图核心模块
│   │   ├── gantt-core.js         #   - 核心类定义与初始化
│   │   ├── gantt-render.js       #   - DOM渲染、滚动同步、侧边栏拖拽
│   │   ├── gantt-dependencies.js #   - 依赖计算、折叠聚合、冲突检测
│   │   └── gantt-operations.js   #   - 任务增删改查、WBS生成、层级管理
│   │
│   ├── events/                   # 🖱️ 事件处理模块
│   │   ├── gantt-events-binding.js # - 基础事件绑定（点击/双击）
│   │   ├── gantt-events-drag.js    # - 拖拽移动、调整时长
│   │   ├── gantt-events-form.js    # - 内联编辑表单、依赖选择器
│   │   └── gantt-events-quickmenu.js # - 悬停快捷菜单
│   │
│   ├── app/                      # 🚀 应用层模块
│   │   ├── app-init.js           #   - 应用启动、初始数据加载
│   │   ├── app-controls.js       #   - 工具栏按钮逻辑
│   │   ├── app-file-manager.js   #   - 云端文件管理 UI 逻辑
│   │   ├── app-view-switcher.js  #   - 视图切换 (Gantt <-> PERT)
│   │   └── app-settings.js       #   - 设置面板逻辑
│   │
│   ├── pert-chart.js             # 🕸️ PERT 核心渲染
│   └── pert-interactive.js       # 🕸️ PERT 交互逻辑（连线、拖拽）
│
└── functions/api/                # ☁️ Cloudflare Workers 后端
    ├── list.ts                   # 获取文件列表
    ├── save.ts                   # 保存/覆盖文件
    ├── load.ts                   # 加载文件内容
    └── delete.ts                 # 删除文件
```

### 模块加载与依赖关系

```text
┌─────────────────────────────────────────────────────────┐
│                    index.html                           │
│               (模块加载器 + 页面结构)                    │
└────────────────────┬────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │  按依赖顺序加载核心层级  │
        └────────────┬────────────┘
                     │
    ┌────────────────┼────────────────┐
    │                │                │
┌───▼────┐      ┌───▼────┐      ┌───▼────┐
│ 第1层  │      │ 第2层  │      │ 第3层  │
│工具/API│ ───> │甘特核心│ ───> │事件/交互│
└────────┘      └────────┘      └────────┘
    │                │                │
    │           ┌────▼────┐      ┌───▼────┐
    │           │ 第4层   │      │ 第5层  │
    └──────────>│PERT模块 │ ───> │应用层  │
                └─────────┘      └────────┘

详细依赖链更新 (Epsilon 18)：
第1层: date-utils, kv-storage, time-scale-utils 等基础工具
第2层: gantt-core → gantt-render → gantt-dependencies (核心逻辑)
第3层: gantt-events-* (拖拽、表单、菜单)
第4层: pert-chart.js + pert-interactive.js (依赖第1、2层数据结构)
第5层: app-* (整合所有功能，UI控制)
```

</details>

---

<details>
<summary>🎨 <strong>视觉设计与交互亮点</strong></summary>

### 工具栏设计 (UI 重构)
<details>
<summary>🎯 呼吸灯折叠按钮</summary>

- **状态**: 默认收起为一个圆形按钮 (56px)。
- **动画**: 带有脉冲 (Pulse) 和微光 (Shimmer) 动画，表示系统就绪。
- **交互**: 鼠标悬停时，向右平滑展开毛玻璃工具栏。

</details>

<details>
<summary>🌈 功能区划分</summary>

| 分区 | 功能 | 图标 | 颜色逻辑 |
|------|------|------|----------|
| **文件操作** | 管理/导出/加载 | 📁 💾 | 蓝色/紫色系 (数据) |
| **任务编辑** | 添加/删除 | ➕ 🗑️ | 绿色(增)/红色(删) |
| **智能工具** | 冲突检测/修复 | 🔍 🔧 | 橙色/青色 (诊断) |
| **视图控制** | PERT/刻度切换 | 🔄 📅 | 粉色/灰色 (视图) |

</details>

### PERT 视图交互 (Enhanced)
- **手柄连线**: 节点右侧手柄拖拽至另一节点左侧，可视化建立依赖。
- **状态反馈**: 拖拽过程中，目标节点高亮，连线显示为虚线。
- **全貌模式**: 自动计算缩放比例，一键查看完整的网络图。

### 甘特图交互细节
- **侧边栏拖拽**: 鼠标悬停在任务名称栏右边缘，可拖拽调整侧边栏宽度。
- **全貌视图**: 智能计算项目总工期和屏幕宽度，自动调整单元格密度。
- **双层标签**: 任务条左侧显示开始时间，右侧显示名称+结束时间+进度。

</details>

---

<details>
<summary>🔧 <strong>核心功能演进 (Epsilon 18)</strong></summary>

### ☁️ Cloudflare KV 云存储
*   **无服务器后端**: 使用 Cloudflare Pages Functions 实现 API。
*   **文件管理**: 支持云端文件的列表查看、保存、覆盖、删除。
*   **元数据**: 保存时记录文件大小、任务数量、时间戳。
*   **自动降级**: 若云端 API 不可用，自动切换为本地 JSON 文件下载/加载。

### 🔗 智能依赖管理 (`gantt-dependencies.js`)
*   **折叠聚合 (Aggregated Dependencies)**: 
    *   当父任务折叠时，其子任务对外的依赖关系不会消失。
    *   系统会自动计算并将依赖线“上浮”连接到父任务，显示为**紫色虚线**。
*   **有效性验证**: 
    *   防止循环依赖。
    *   防止父子任务间的非法依赖。
*   **冲突检测**: 实时检测 `Start Date <= Dependency End Date` 的逻辑错误。

### 📅 双模式工期计算 (`date-utils.js`)
*   **工作日模式 (Workdays)**: 
    *   自动跳过周末（周六、周日）。
    *   工期 3 天 = 周五 + 周一 + 周二。
*   **自然日模式 (Days)**: 
    *   包含周末。
    *   工期 3 天 = 周五 + 周六 + 周日。
*   **混合支持**: 不同任务可以分别设置不同的工期模式。

### 🔍 全貌视图 (Overview Mode)
*   **算法**: `calculateOverviewParams` 
*   **逻辑**: 
    1. 计算项目最早开始和最晚结束时间。
    2. 获取容器可视宽度，扣除侧边栏和预留空间。
    3. 动态计算最佳 `cellWidth` (像素/天)。
    4. 自动选择最合适的时间刻度 (日/周/月)。

</details>

---

<details>
<summary>⚙️ <strong>设置与配置</strong></summary>

### 侧滑设置面板
- **编辑控制**: 启用/禁用拖拽移动、启用/禁用调整时长。
- **显示控制**: 显示/隐藏周末、依赖箭头、操作日志。
- **视图密度**: 滑动调整时间轴密度 (40px - 80px)。

### 操作日志
- **实时记录**: 记录所有增删改查、依赖变更、冲突修复操作。
- **防抖写入**: 50ms 内的日志合并写入，防止 DOM 操作频繁。
- **折叠/展开**: 支持最小化，以免遮挡视线。

</details>

---

<details>
<summary>📊 <strong>数据结构说明</strong></summary>

### 任务对象 (Task Object)
```javascript
{
  "id": "task-1732268...",      // 内部唯一ID (String)
  "uid": 1,                     // 易读序号 (Number)
  "name": "网站设计",            // 任务名称
  "start": "2025-11-21",        // YYYY-MM-DD
  "end": "2025-11-25",          // YYYY-MM-DD
  "duration": 5,                // 工期数值
  "durationType": "workdays",   // "workdays"(工作日) | "days"(自然日)
  "progress": 65,               // 0-100
  "outlineLevel": 1,            // 缩进层级 (1, 2, 3...)
  "wbs": "1.1",                 // WBS 编号
  "isSummary": false,           // 是否为汇总任务
  "isMilestone": false,         // 是否为里程碑
  "isCollapsed": false,         // 折叠状态
  "parentId": null,             // 父任务ID
  "children": [],               // 子任务ID数组
  "dependencies": [             // 依赖数组
    { "taskId": "task-abc...", "type": "FS", "lag": 0 }
  ]
}
```

### 云端存储结构 (KV)
- **Key**: 文件名 (e.g., `project-alpha.json`)
- **Value**: JSON 字符串 (包含 `project` 信息和 `tasks` 数组)
- **Metadata**: 
  ```json
  {
    "timestamp": 1732268000000,
    "size": 4096,
    "taskCount": 15
  }
  ```

</details>

---

<details>
<summary>🛠️ <strong>开发与部署</strong></summary>

### 模块加载流程
1. `index.html` 初始化，动态加载 CSS。
2. 按顺序加载 JS 模块：
   - Utilities (Level 1)
   - Gantt Core (Level 2)
   - Events (Level 3)
   - PERT (Level 4)
   - App Controllers (Level 5)
3. 执行 `checkModulesLoaded` 进行 7 项核心模块验证。
4. 加载 `initial-tasks.json` 或初始化空项目。

### Cloudflare Pages 部署
若要使用云端存储功能，需配置 `wrangler.toml`：

```toml
name = "ywgantt"
pages_build_output_dir = "./"
compatibility_date = "2023-01-01"

[[kv_namespaces]]
binding = "GANTT_STORAGE"
id = "YOUR_KV_NAMESPACE_ID"
```

API 路由位于 `functions/api/` 目录，Cloudflare Pages 会自动识别并部署为 Worker Functions。

</details>

---

Generated for YW Gantt Project - Version Epsilon 18
